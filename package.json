{
  "name": "berlinjs",
  "description": "Berlinjs website",
  "repository": "yoshuawuyts/berlinjs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps && nyc node test.js",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov"
  },
  "dependencies": {},
  "devDependencies": {
    "choo": "^5.0.0-5",
    "dependency-check": "^2.8.0",
    "i": "^0.3.5",
    "nyc": "^10.1.2",
    "standard": "^9.0.1",
    "tachyons": "^4.6.2",
    "tape": "^4.6.3"
  }
}
